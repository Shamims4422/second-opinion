# All settings are optional; defaults work out of the box.
# Copy to .env to override.

# CRITICLOOP_DATABASE_URL=sqlite:///./criticloop.db
# CRITICLOOP_EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
# CRITICLOOP_MIN_SIMILARITY=0.3
# CRITICLOOP_RETRIEVAL_LIMIT=5
# CRITICLOOP_APPROVE_THRESHOLD=0.75
# CRITICLOOP_BLOCK_THRESHOLD=0.45
# CRITICLOOP_CORS_ORIGINS=["http://localhost:8000","http://127.0.0.1:8000"]
